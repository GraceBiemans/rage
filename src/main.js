import { createApp } from 'vue';  // Import Vue 3's createApp function
import App from './App.vue';
import './styles.css'  // or './styles.css'

createApp(App).mount('#app');  // Mount the App component to the DOM